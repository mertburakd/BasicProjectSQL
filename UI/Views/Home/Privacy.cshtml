@model List<UI.Models.Entities.Test>
@{
    ViewData["Title"] = "Privacy Policy";
}
<h1>@ViewData["Title"]</h1>

<p>Use this page to detail your site's privacy policy.</p>

@foreach (var item in Model)
{
    <p>@item.Id</p>
    <p>@item.Names</p>
}
